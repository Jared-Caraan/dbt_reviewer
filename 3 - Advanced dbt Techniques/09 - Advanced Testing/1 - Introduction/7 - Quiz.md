What is the intent of testing the uniqueness of a column in a dbt model?

a. To ensure all values in the column are numeric.\
b. To confirm that there are no duplicate values in the column.\
c. To verify the column is not null.\
d. To identify the grain of the table.

Answer: b

##
Which command will automatically run both models and tests in DAG order in dbt?

a. `dbt run`\
b. `dbt seed`\
c. `dbt build`\
d. `dbt test`

Answer: c

##
What is a key characteristic of an effective test in succesful dbt test?

a. It should run manually every time.\
b. It should generate as many results as possible.\
c. It should run automatically and be fast.\
d. It should cover multiple aspects in one test.

Answer: c

##
Which type of test checks the referential integrity between two models??

a. Not null Test.\
b. Uniqueness Test.\
c. Relationship Test.\
d. Accepted values Test\

Answer: c
