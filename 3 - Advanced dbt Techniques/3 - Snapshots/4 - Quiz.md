What is the primary purpose of dbt snapshots?

a. To perform incremental model updates.\
b. To create real-time streaming pipelines.\
c. To create materialized views of data.\
d. To track changes in source data over time

Answer: d

##
What column does dbt automatically add to snapshot tables to indicate the current active record?

a. updated_at\
b. is_active\
c. dbt_valid_to\
d. change_tracker

Answer: c

When using the timestamp strategy for dbt snapshots, which column is required in the source table?

a. updated_at\
b. created_at\
c. snapshot_id\
d. dbt_change_tracking\

Answer: a
